div(class="liveplayer")
    img(src='{{ image }}')
    div(id='play_controller', ng-show='image!==undefined && !loading')
        div(class="osd")
            button(id='play', ng-click="playing=true", ng-disabled="playing", title='Play')
                span(class='glyphicon glyphicon-play')
            button(id='stop', ng-click="playing=false", ng-disabled="!playing", title='Stop')
                span(class='glyphicon glyphicon-stop')
            
        button(id='osd', ng-click="toggleOsd()", class="btn btn-sm btn-default hidden-xs hidden-sm", title='Toggle osd', data-toggle="button")
            span(class='glyphicon glyphicon-sunglasses')

    div(ng-show='loading', class="progress-bar progress-bar-striped active",aria-valuenow="100",aria-valuemin="0", aria-valuemax="100" style="width: 100%") Loading